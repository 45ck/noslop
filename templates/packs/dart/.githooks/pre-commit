#!/bin/sh
# noslop pre-commit: fast gates (format, analyze)
set -e

if command -v noslop >/dev/null 2>&1; then
  noslop check --tier=fast
else
  dart format --output=none --set-exit-if-changed . && dart analyze
fi
