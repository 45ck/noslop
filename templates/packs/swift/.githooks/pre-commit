#!/bin/sh
# noslop pre-commit: fast gates (swift-format, swiftlint)
set -e

if command -v noslop >/dev/null 2>&1; then
  noslop check --tier=fast
else
  swift-format lint --recursive . && swiftlint lint
fi
