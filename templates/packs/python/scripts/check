#!/bin/sh
# noslop check script for Python
TIER="${NOSLOP_TIER:-${1:-fast}}"

case "$TIER" in
  fast)
    black --check . && ruff check .
    ;;
  slow)
    pytest
    ;;
  ci)
    black --check . && ruff check . && pytest
    ;;
  *)
    echo "Unknown tier: $TIER — use: fast, slow, or ci" >&2
    exit 1
    ;;
esac
