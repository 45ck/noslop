#!/bin/sh
# noslop pre-push: slow gates (build + tests)
set -e

if command -v noslop >/dev/null 2>&1; then
  noslop check --tier=slow
else
  cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build && ctest --test-dir build --output-on-failure
fi
