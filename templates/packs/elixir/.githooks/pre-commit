#!/bin/sh
# noslop pre-commit: fast gates (format, credo)
set -e

if command -v noslop >/dev/null 2>&1; then
  noslop check --tier=fast
else
  mix format --check-formatted && mix credo --strict
fi
