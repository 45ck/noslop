#!/bin/sh
# noslop check script — delegates to npm scripts by tier
TIER="${NOSLOP_TIER:-${1:-fast}}"

case "$TIER" in
  fast)
    npm run format:check && npm run lint && npm run spell
    ;;
  slow)
    npm run typecheck && npm run test
    ;;
  ci)
    npm run ci
    ;;
  *)
    echo "Unknown tier: $TIER — use: fast, slow, or ci" >&2
    exit 1
    ;;
esac
