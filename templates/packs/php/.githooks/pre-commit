#!/bin/sh
# noslop pre-commit: fast gates (format, phpstan)
set -e

if command -v noslop >/dev/null 2>&1; then
  noslop check --tier=fast
else
  vendor/bin/php-cs-fixer check . && vendor/bin/phpstan analyse
fi
