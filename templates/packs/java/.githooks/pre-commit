#!/bin/sh
# noslop pre-commit: fast gates (checkstyle, pmd)
set -e

if command -v noslop >/dev/null 2>&1; then
  noslop check --tier=fast
else
  ./gradlew checkstyleMain checkstyleTest -q && ./gradlew pmdMain -q
fi
